language: php
php:
  - '5.6'

install:
  - composer self-update
  - make install

script:
    - make lint
    - mkdir -p build/logs
    - phpunit --coverage-clover build/logs/clover.xml

after-script:
    - php vendor/bin/coveralls -v
